#include <stdio.h>
#include <stdlib.h>


int main() {
	int i = 0, sum = 0, t;
	char c[10] = { 0, };
	char arr[100] = { 0, };
	scanf_s("%d", &t);
	gets(c);
	gets(arr);
	for (i = 0; i < t; i++) {
		sum = sum + arr[i];
	}
	printf("%d\n", sum);
	return 0;
}
